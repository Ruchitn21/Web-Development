<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Random Number</title>
</head>
<body>
    <h1>Generating Random Number</h1>
    <br>
    <!-- <h3>Your random Number is : <%=// Math.floor(Math.random()*10)+1 %></h3> -->
    <h3>Your random Number is : <%= rand %></h3>
    <br>

    <% if(rand%2===0) { %>
        <h4>The Number is Even</h4>
    <% } else { %>
        <h4>The Number is Odd</h4>
    <% } %>
</body>
</html>